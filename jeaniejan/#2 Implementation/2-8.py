#2_8 자물쇠와 열쇠
def solution(key,lock):
    distance=len(key)-1
    for row in range(distance+len(lock)):
        for col in range(distance+len(lock)):
            for _ in range(4):
                arr=
    